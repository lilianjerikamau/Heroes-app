{{#partial "content"}}
    <div class="container">
        <h1>{{hero.name}}</h1>
        <a href="/hero" ><button class="btn btn-primary">Go Back</button></a>
    </div>
{{/partial}}
{{> layout.hbs}}
